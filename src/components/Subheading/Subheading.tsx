import React from 'react';

const SubHeading = ({ title }) => {
    return (
        <div>
            <span className="font-OpenSans-Bold text-lg font-bold text-primary">{title}</span>
        </div>
    )
}
export default SubHeading;